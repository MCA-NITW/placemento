# NPM Configuration for Placemento Frontend
# 
# This configuration helps manage known vulnerabilities in development dependencies
# while maintaining security for production dependencies.

# Audit configuration
# We set audit-level to high for CI/CD to focus on critical production issues
# Development dependency vulnerabilities in react-scripts are documented separately
audit-level=high

# Performance optimizations
engine-strict=true
save-exact=true

# Registry configuration
registry=https://registry.npmjs.org/

# Progress and logging
progress=false
loglevel=warn

# Timing for performance monitoring
timing=true
