# CI Environment Variables for GitHub Actions
# These variables are used during CI/CD pipeline execution

# Database Configuration
DB_CONNECTION_STRING=mongodb://localhost:27017/placemento_test
LOCAL_DB_CONNECTION_STRING=mongodb://localhost:27017/placemento_test

# JWT Configuration
JWT_SECRET=test_jwt_secret_for_ci_only_not_for_production
JWT_EXPIRES_IN=24h

# Email Configuration (Test values for CI)
EMAIL_USER=test@example.com
EMAIL_PASS=test_password
EMAIL_SERVICE=gmail

# Server Configuration
PORT=5000
NODE_ENV=test

# Frontend Configuration
REACT_APP_API_URL=http://localhost:5000

# CI Specific
CI=true
SKIP_PREFLIGHT_CHECK=true
